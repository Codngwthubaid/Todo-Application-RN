// Define user-related types
export interface User {
    // Add your user properties here based on what getCurrentUser returns
    id: string;
    name: string;
    email: string;
    // Add other user properties as needed
  }